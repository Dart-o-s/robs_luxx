// some meta tokens
// they are interpreted either by the scanner already, the parser, resolver or interpreter
// the names are usually self explaining

var a = (1 + 2) * (4 + 44) / 22;

print a;

// the parser is supposed to stumble over this
// and print the ast
_meta::printAst and this is the message, everything after the meta token is eaten by the scanner and is printed out!

print "this was not in the ast yet";