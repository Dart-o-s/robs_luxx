class Mio {
  init(last) {
    this.last = last;
  }

  salute() {
    print "Hello: " + this.name;
    print "Last: " + this.last;
  }
}

class Tuyo < Mio {
  cheers() {
    print "Say what?";
  }
}

// print Mio;

// var inst = Mio("Oliver");
// print inst;

// inst.any = "wow";
// print inst.any;

// inst.name = "Robert";
// inst.salute();

var child = Tuyo("Jackson");
child.cheers();
child.name = "John";
child.salute();