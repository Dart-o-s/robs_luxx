///
/// We persist the token stream that let to this "universe"
/// After reloading the compiler should be able to reconstruct
/// the classes, and actually the whole world.
/// The tokens are only loaded, and if we indeed parse them:
///   - they are still not interpeted
///   - hence no big deal to parse them
///

class TokenStreamWriter {

}